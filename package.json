{
  "name": "orangehrm-test-automation-platform",
  "version": "1.0.0",
  "description": "Enterprise Playwright automation framework for OrangeHRM — layered architecture with App facade and Builder pattern",
  "author": "QA Engineering Team",
  "license": "MIT",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --project=ui-tests",
    "test:api": "playwright test --project=api-tests",
    "test:headed": "playwright test --headed --project=ui-tests",
    "test:debug": "playwright test --debug --project=ui-tests",
    "test:ci": "playwright test --reporter=list",
    "clean": "rimraf test-results playwright-report"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@faker-js/faker": "^9.4.0",
    "@playwright/test": "^1.50.1",
    "@types/node": "^22.13.4",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  }
}
